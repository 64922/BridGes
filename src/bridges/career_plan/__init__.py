"""职业规划模块子图（V2 Issue 15）。

模块只由日常模式中随用户消息持久化的 ``module_id=career`` 或用户点击的
一键建议启动，绝不在普通聊天里暗中检索。节点顺序：
``career.parse → career.plan → career.collect → career.filter → career.analyze
→ career.advise``。

岗位名是检索锚点：原词逐字保留，只把**真正同义**的说法当作同一岗位，相邻
岗位（例如以「Java 后端实习」为目标时的前端、测试岗位）单列建议，绝不暗换
成目标岗位。主样本只纳入**公开可读且岗位与城市都匹配**的岗位；样本不足时
停止总体推断，不把本轮检索所得称为全国市场均值。

注意区分历史能力：``bridges.career`` 是旧产品的「生涯规划助手」（Issue 29，
模型驱动的规划对话，随 Issue 21 退役）；本包是 V2 职业规划模块，与其没有
共享状态，也不调用模型。

（包初始化保持轻量：``bridges.contracts.chat`` 会导入本包的契约模块，
在包初始化里导入编排服务会形成循环。）
"""
