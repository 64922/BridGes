# 来源清洁记录（净室审计）— bridges-humanizer

记录日期：2026-08-10
记录人：bridges-humanizer 实现者（BridGes 项目）

## 声明

本 SKILL 的全部规则、提示、示例、体裁合同、事实锁规则、代码与测试资产
均为原创，由 BridGes 项目独立编写，未复制任何外部参考项目的文本、提示、
目录结构或示例。

## 外部参考的处理方式

| 外部项目 | 许可证 | 处理方式 |
|----------|--------|----------|
| `scientific-humanization` | 未声明 | 仅作为**方法研究方向**参考其公开方法学论文/文档中的通用方法（如“输出包含修改明细与理由”这一通用工作流概念），未复制其任何文本、提示、结构或示例；本仓库不包含其任何内容 |
| `Humanizer-zh` | MIT | **零复用**：未复制其任何代码或文本，未引用其示例；其存在仅记录于此，以防未来误用 |

## 通用方法吸收（不构成版权主张）

以下通用做法不受版权保护，属行业惯例，可自由使用：
- “改写前先明确任务目标、受众与约束”的写作流程。
- “输出包含最终文本、修改明细、理由、核查结果”的结构化交付思路。
- “对数值、单位、限定条件做前后一致性检查”的工程方法。

## 审计结论

- 本 SKILL 内不存在任何来自外部项目的代码、文本、提示或示例。
- 若未来确需复用任何 MIT 许可材料，必须按 ADR 0011 保留许可证、作者
  署名与本记录的更新条目，方可合入。
- 本记录随 SKILL 版本 1.0.0 固化，任何内容变更须同步更新此记录与版本。

## Issue 07 路由增量记录（2026-08-10）

## Issue 17 全局轻量表达策略增量记录（2026-08-10）

本次新增 `global-humanized-writing-v1`，仅用于普通陪伴、学习、搜索摘要和能力结果说明的自然语言正文；策略在原有唯一模型生成调用中注入，不新增评分器、改写器或第二次模型调用。Issue 07 的文章人味化仍保留完整任务契约、事实锁和结构化结果，最终文章不经过本策略二次改写。

本次实现为 BridGes 独立重写的原创规则、快照模型、受保护区恢复逻辑和测试夹具。参考记录中的 `scientific-humanization`、`cognitive-profile`、`Humanizer-zh`、`human-writing` 与 `nuwa-skill` 仅用于研究边界或清洁室审计；未复制其代码、提示词、文本、示例、测试、素材或结构，默认零复用。未声明许可的研究材料不作为实现来源；MIT 项目仍不构成本次代码或文本的输入来源。

运行审计、模型运行锁和对外运行视图仅保存策略版本、模式、最小画像切片标识、条目数量与降级原因等脱敏元数据；为保证重试稳定，内部 generation run 配置另保存本次不可变快照，既不向前端暴露，也不进入审计。代码、公式、JSON、引用、链接、确定性错误、加载/停止状态、工具原始结果和协议字段保持原契约，工具结果只允许额外附带简短说明。缺少策略资源或画像切片时使用安全基线；重试复用同一快照，避免热更新漂移。

验收证据：`tests/chat/test_global_writing_policy.py` 覆盖模式切片、资源缺失、快照稳定性、保护区恢复和唯一提示词组装；`tests/skills/test_skill_registry.py` 覆盖能力注册中的策略版本。

本次新增的 `humanizer-route-v1`、自然语言命中规则、中文默认值、任务合同
字段、测试样例与聊天接入代码均由 BridGes 独立编写；没有从参考项目逐字
复制代码、提示、规则、示例、评测语料或静态资源。内置 SKILL 版本仍为
1.0.0，路由版本是独立的控制平面合同版本。

| 外部参考项目 | 许可证判断 | 本次处理 |
|--------------|------------|----------|
| `C:\Users\33755\Desktop\参考资料\Humanizer-zh-main` | 本次未复用 | 未把其代码、文本、提示、测试或资源带入仓库 |
| `C:\Users\33755\Desktop\参考资料\human-writing-main` | 本次未复用 | 未把其代码、文本、提示、测试或资源带入仓库 |
| `C:\Users\33755\Desktop\参考资料\scientific-humanization-main` | 未以其内容作为许可来源 | 仅保留用户指定的研究对象名称；未复制其内容或结构 |

原创增量包括：十种正向自然语言表达、十种相似但不应命中的表达、
“面向/体裁/渠道/长度/必须保留项”的合同编译规则、缺少原文的稳定提示、
知识库同名文档的账户内精确证据选择，以及历史聊天附件不可作为新路由重试
输入的边界。审阅证据为 `tests/humanizer/test_intent.py`、人味化服务合同
测试与聊天生命周期测试；自然语言路由的外部证据开关、未知版本拒绝和单次
生成门也有确定性测试；测试中没有来源不明的逐字片段。

新增 `fixtures/natural_language_eval.md`：30 条原创中文盲评样例，覆盖说明文、
科普、邮件、报告、演讲稿以及短、中、长文本，并冻结自然、具体、无模板腔量表。

## Issue 07 方法体系与全局策略升级增量记录（2026-08-11）

本次研读仓库外只读目录中的 `Humanizer-zh-main`、`human-writing-main` 与
`scientific-humanization-main`，仅吸收通用方法方向：识别表达模板、调整中文句法
节奏、按证据强度收敛 claim，以及区分聊天和文章场景。没有复制上述项目的代码、
正文、提示词、示例、目录结构、测试或评测语料；`SKILL.md` 中的规则、对照例句和
规则标识均由 BridGes 独立重写。

净室自查证据（2026-08-11）：对上述三个只读目录中的 Markdown、文本和 Python
文本块，与本次新增 `SKILL.md`、`method_rules.py` 中长度不少于 80 字符的 34 个
段落做整段精确匹配，结果为 `EXACT_PARAGRAPH_OVERLAPS=0`。该脚本只读文件，
不把参考项目内容写入仓库。

新增 `method_rules.py` 作为方法规则的单一事实源，文章改写/生成提示词和全局聊天
策略都从这里渲染；`tests/humanizer/test_method_rules_consistency.py` 通过规则 ID
检查文档、常量和提示词组装不会再次漂移。`SKILL.md` 补充 frontmatter、检测面、
改写面、事实护栏面、三场景档位、个性与分寸和人味分档/教学例子两项原创规则，
并同步记录草稿事件、硬门/软门、一次定向修复与 WARN 交付语义。

普通聊天策略从 `global-humanized-writing-v1` 升级为
`global-humanized-writing-v2`，聊天档方法块（含修辞频率告警）由同一规则模块
注入；受保护区、安全基线和“文章人味化最终文章不经过全局策略二次改写”保持不变。
本次没有新增模型调用、逐条拦截器或用户画像推断；版本变更只描述方法合同升级。

## Issue 04 首稿 profile 与表达审稿器增量记录（2026-08-12）

本次重建文章首稿生成与生成后表达审稿：新增 `draft_compiler.py`（三档正向
生成 profile：轻度/标准/深度，每次只编译 6—10 条高优先级规则）、
`expression_review.py`（16 类稳定 code 的软审稿器）、
`contracts/expression_review.py`（版本化 `ExpressionReviewReport`），并把
四类体裁 profile 从「必现元素正则」改为「任务目标/风险/可选表达方式」。

从参考项目只吸收以下通用写作思想（不构成版权主张，也未复制任何文本、
规则、提示或示例）：

- 「材料门槛」：正文只能消费账本材料，材料不足时追问或占位，不编造。
- 「说话位置」：写作前先确定谁在说、凭什么知道。
- 「先成稿再审」：先交付正文，再对正文做只报警的静态检查。

本 Issue 新增的全部实现、规则词表（抽象包装词、商业隐喻词、假具体模式、
场景词等）、三档 profile 的规则选择、16 类发现 code 的中文解释与定向建议、
体裁 profile 的任务目标/风险/可选表达、测试夹具与提示结构均为 BridGes
原创独立编写；没有复制参考项目的代码、提示词、示例或测试资产。

净室自查证据（2026-08-12）：新增 `expression_review.py` 与 `draft_compiler.py`
中对参考项目内容无逐段引用；审稿词表与发现解释全部按 BridGes 自身语料和
中文写作常识独立编写，未从任何外部项目截取。
