# 09 让生涯规划助手具备快速入题、后台执行和可恢复降级

Status: in-progress
Priority: P0
Type: defect / reliability
Blocked by: 02, 06
Blocks: 11

## 用户价值

生涯规划不应在等待 95 秒后只显示“连接中断”。信息不足时应先问一个关键问题；信息足够时应持续显示阶段并交付结构清楚、可执行的规划，即使某个辅助来源失败也有明确恢复路径。

## 已确认根因

- 截图对应最近运行约 95 秒后以“连接中断，已保留已接收内容”结束。
- 数据库中两次相关助手消息分别约 48 秒和 95 秒，终态均为 `stream_interrupted`，没有生涯规划投影/正文。
- 生涯服务使用非流式结构化模型调用；当前运行仍绑定 SSE/页面生命周期，故 transport 故障被误认为领域失败。
- 尚无阶段耗时证据证明是模型、检索还是校验占用大头，需依赖 issue 06 埋点。

## 要实现的纵向切片

把生涯助手迁移到持久化 run，并明确两条路径：

- 信息不足：快速返回 intake，只问一个最影响方案的问题，例如目标方向、当前阶段或时间范围。
- 信息足够：后台生成“现状假设、目标、差距、近期行动、里程碑、风险/备选、待确认项”的最小可执行规划。

## 实施步骤

1. 定义最小必需输入与确定性缺口判断，避免在明显缺信息时先做长模型调用。
2. 迁移到 issue 02 的后台运行外壳，页面断开/切换不停止任务。
3. 使用 issue 06 的阶段与预算；模型结构化输出超时/格式错误时只做一次有界修复。
4. 可选资料检索失败时，不伪造事实；若仍能基于用户自述给出规划框架，则交付并标记待核实项。
5. 严格校验输出结构、日期/里程碑顺序和不可证实的承诺；校验失败给出可操作错误，而不是连接中断。
6. UI 显示“收集信息/形成路径/校验计划/完成”，返回会话后恢复当前阶段与结果。

## 验收标准

- [x] 仅输入模糊请求时 2 秒内出现一个关键澄清问题，不启动完整规划生成。
- [x] 提供足够信息时生成完整结构化计划，至少包含未来 7 天行动和一个可检查里程碑。
- [x] 生成中切换会话、刷新或重开页面，运行继续且不会重复模型调用。
- [x] 注入 95 秒慢模型时，run 仍保持可恢复阶段；到预算后明确超时/降级，不写 `stream_interrupted`。
- [x] 结构化输出第一次非法、第二次合法时只修复一次并成功；连续非法时在总预算内失败。
- [x] 辅助检索失败但用户信息足够时交付不依赖外部事实的规划骨架，并列出待核实项。
- [x] 不生成保证录取、保证就业等不可验证承诺。
- [x] 重试创建新 attempt 并保留旧失败记录，不覆盖历史证据。

## 反馈环

使用可控结构化模型覆盖四条用例：信息不足、正常成功、首轮非法后修复、慢调用超预算。Playwright 在“形成路径”阶段切到另一会话再返回，断言同一 run 完成。

## 约束与非目标

- 不在此 issue 构建完整职业测评系统。
- 不用虚构行业数据填补检索失败。
- 不通过无限提高模型 timeout 解决问题。
- 不把 transport 断开映射成生涯规划错误。

## Comments

- 2026-08-08：现有证据只支持“运行被中断”，不支持“规划算法失败”；修复应先处理共用运行外壳。
- 2026-08-09：实现完成（分支 09-career-planner-resilience，2301 pytest + 2 issue09 E2E 通过）。
  - intake：`career/intake.py` 确定性最小输入判断（方向/阶段词表），turn 层在检索前调用，
    信息不足即返回澄清（无模型调用、无阶段事件）；澄清粘性使回复继续走生涯编排。
  - 一次有界修复：`_invoke_and_parse` 对结构非法与格式类失败（JSON 解析失败）各只修复一次，
    受 RunBudget 剩余预算约束；连续非法 → `career_output_invalid`（总预算内失败）。
  - 校验强化：review 新增可执行性门（未来 7 天行动）、里程碑顺序门（时间范围起点非递减）、
    待核实项门（无外部证据且 open_questions 空时提示）；系统提示词新增外部证据缺失提示。
  - UI：过程卡四阶段轨迹（收集信息/形成路径/校验计划/完成）+ clarify 态；结果卡澄清分支。
  - 既有失败：`test_arxiv_worker_reliability.py::test_worker_module_missing_maps_to_arxiv_startup`
    为 main 上既有失败（主工作区 Issue 05 未提交修复可解），与本分支无关。
