# 使用账户级百炼密钥与真实能力探测

BridGes 的百炼 API Key 只能由登录用户在界面中为当前账户配置：原生运行使用操作系统凭据库，Docker 使用由应用自动生成主密钥保护的加密凭据卷，均不要求 `.env`。缺少或验证失败的密钥只阻断 AI 能力，不阻断登录、密钥设置、资料管理和数据导出；保存密钥后以非用户数据探测每项固定模型能力并显示真实状态，生产环境不得注册会伪装成功的 Stub。
