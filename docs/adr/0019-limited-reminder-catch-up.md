# 本地提醒采用 24 小时有限补发

BridGes 只在本地调度器随 `BridGes start` 或容器服务运行时发送 QQ 邮件，不注册操作系统常驻服务，也不承诺程序完全关闭期间投递。计划同时保存账户时区规则和 UTC 执行时间；恢复运行后，24 小时内错过的一次性提醒立即补发并标记延迟，超过窗口的任务记为已错过且只允许手动补发。重复提醒最多补发最近一次，跳过更早积压并继续下一正常周期。SMTP 临时失败仅执行有限退避重试，授权失效立即停止并提示重新验证；发送、跳过、失败、补发和手动重试均进入账户级投递记录。
