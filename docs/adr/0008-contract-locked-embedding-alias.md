# Embedding 使用受合同约束的稳定模型 ID

百炼未为 `text-embedding-v4` 提供日期快照，而自托管会破坏“一个百炼 Key 调用全部模型”的交付约束，因此 BridGes 固定使用 `text-embedding-v4` 与 1024 维作为模型快照规则的唯一技术例外。索引必须锁定模型 ID、维度、规范化、分块器和 Schema；能力合同变化时禁止继续混写，需构建并验证新索引后原子切换，旧索引保留至回滚窗口结束。
