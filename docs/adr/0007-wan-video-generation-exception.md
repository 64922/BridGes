# 文生视频采用万相 Wan 作为唯一系列例外

百炼当前没有可用于项目文生视频的 Qwen 系列模型，因此 BridGes 将 `wan2.7-t2v-2026-06-12` 作为“除搜索外均采用 Qwen”原则的唯一模型系列例外。该模型仍使用同一全局百炼运行凭据（ADR-0024），遵守单类别单快照、用户不可更改、不可用即明确停用的共同合同；账户级能力探测已随 GQ-06/GQ-07 移除。其他智能模型类别继续采用 Qwen 系列。
