# BridGes

BridGes 是面向长期科学学习、科学表达与成长陪伴的聊天优先应用，目标是在持续互动中同时提升用户的科学理解、表达质量与事实可靠性。

> 当前产品合同：以 [ADR-0026](docs/adr/0026-frozen-product-contracts-and-migration-gates.md) 和 [.scratch/final/PRD.md](.scratch/final/PRD.md) 为准。旧 ADR、根级计划和历史任务只用于审计；若与本处或 ADR-0026 冲突，不得作为当前实现依据。

## Language

**长期科学学习与表达伙伴**：
本项目的产品主轴。它持续理解用户的知识基础、认知偏好、表达风格、研究兴趣与成长目标，并辅助用户理解科学及产出可靠、自然、适配受众的科学内容。
_Avoid_: 通用聊天机器人、单次问答工具、功能拼盘

**核心用户**：
大学生、研究生、青年科研人员及深度科学爱好者。教师与科普创作者属于扩展角色，不作为另一套割裂产品的主轴。
_Avoid_: 无差别的全年龄用户

**科学成长循环**：
产品的总工作流：提出问题或导入材料，识别目标与基础，组织可信证据，生成个性化解释和多模态展示，协作完成科学表达，执行事实与人味双重校验，再根据用户反馈修订回答或画像。
_Avoid_: 彼此割裂的问答、写作、画像和知识库功能

**有人味表达**：
在保持科学判断、证据强度、限定条件和引用关系的前提下，符合真实任务、目标受众、中文表达习惯与用户授权偏好的科学表达。
_Avoid_: 规避 AI 检测、冒充真人、名人模仿、牺牲事实的润色

**表达任务契约**：
一次科学表达任务对目标、受众、体裁、渠道、长度、风险、必需判断和成功标准的共同约定。
_Avoid_: “写得自然一点”式模糊提示、无受众的通用改写

**事实锁**：
从已验证科学判断及其证据编译出的不可越界信息集合，包括数值、单位、对象、关系、限定条件、结论强度、术语、公式和引用。
_Avoid_: 普通关键词表、让模型自行判断的事实摘要

**体裁契约**：
某类科学表达必须包含、允许省略、可以采用和明确禁止的内容与表达规则，以及对应的人工责任。
_Avoid_: 所有场景共享的单一润色模板

**BridGes 主对话**：
用户登录后的唯一默认入口，在同一对话系统中承载日常陪伴与学习模式，并按意图调用科学检索、表达、规划和多模态能力。
_Avoid_: 全局科学伙伴、项目工作台首页、功能仪表盘

**日常陪伴**：
以理解、陪伴和自然交流为主的聊天模式；可以按自然语言意图调用受控能力，但不会自动创建或推进教学计划。
_Avoid_: 通用无边界聊天、每回合重复选择模式、把陪伴回答伪装成正式教学

**学习模式**：
以学习目标、知识状态、教学计划、课时、测验和证据门为约束的教师式聊天模式；证据不足时自动联网，失败时不伪造或推进计划。
_Avoid_: 固定课程工作台、没有证据的教学结论、用学习项目替代聊天教学

**全局知识库**：
当前账户唯一的文件和图片上传、解析、索引与本地检索来源；产品界面简称“知识库”，实际采用材料时通过紧凑引用定位。
_Avoid_: 聊天附件、项目文件、局部知识库或跨账户对象进入新上传和检索路径

**局部知识库**：
退役术语。历史局部索引只读保留供迁移审计或兼容渲染，不再作为用户上传、检索或权限边界。
_Avoid_: 为学习项目或单个聊天重新创建局部知识库

**学习项目**：
退役术语。历史项目文件按账户迁入全局知识库，历史项目对话解除项目归属；项目名称和关联只保留在迁移审计与历史引用中。
_Avoid_: 把学习项目作为新上传、检索、教学计划或独立工作台入口

**聊天主界面**：
由可折叠侧边栏、对话固定模式、消息流、对话输入和按需结果详情组成的稳定产品框架；首条用户消息提交后不得切换模式。
_Avoid_: 项目主壳、各功能各自独立的应用

**能力结果详情**：
嵌入对话并可按需展开的结构化结果，用于呈现工具进度、依据、引用、产物状态和必要操作，而不成为独立工作台。
_Avoid_: 任务舞台、原始模型思维链、不可操作的加载动画

**思考摘要**：
回答生成时自动展开、完成后折叠为“已思考（用时 X 秒）”的步骤与依据摘要，用户可随时再次展开查看。
_Avoid_: 原始模型思维链、隐藏提示词、逐令牌内部推理

**回答朗读**：
用户主动对单条回答启动的语音合成播放，支持暂停、继续和停止，不改变文本对话的输入与回复流程。
_Avoid_: 实时语音对话、自动播放、持续监听、用 TTS 代替听写

**回答上下文说明**：
解释一次回答使用了哪些画像切片、证据、能力和授权，以及仍缺少哪些质量条件的渐进披露内容。
_Avoid_: 上下文检查器、完整画像面板、开发调试日志

**科学出处图**：
按需展示来源、Claim、事实锁、教学计划、表达或多模态产物、质量门与版本关系的项目专业视图，并提供列表式等价路径。
_Avoid_: 默认首页、装饰性知识图谱、没有精确定位和状态的关系图

**科学媒体对象**：
将原始与派生资产、科学判断、数据、公式、可编辑源、渲染产物、无障碍替代和验证记录组成的版本化多模态作品。
_Avoid_: 只有 PNG、MP4 或音频的不可追溯成品

**结构化分镜**：
在生成动画或复杂交互前形成的可审查媒体设计，明确教学目标、对象、布局、状态、时间、变换、旁白及其科学判断绑定。
_Avoid_: 直接由一句提示词生成不可解释代码

**本地优先混合架构**：
用户画像、长期记忆、私人材料、会话原文、本地知识库索引与撤回记录默认加密保存在用户设备；云端只接收当前任务所需的最小上下文，并承担 Qwen 推理、联网检索及可选协作服务。
_Avoid_: 默认集中上传全部私人数据、依赖云端账号才能使用核心本地能力

**个人保险库运行时**：
在用户从源码创建的独立 Conda 或 `.venv` 环境中运行、绑定账户与设备密钥的本地进程，负责加密保存个人保险库正文、私人索引、离线操作和任务切片授权；设备副本不因此成为独立权威域。
_Avoid_: 浏览器可任意读取的本地文件夹、无账户作用域的单机数据库、依赖系统全局且未经锁定的运行包

**领域模块化单体**：
以稳定领域接口组织身份授权、协作、画像学习、科学证据、创作、工作流和评测等深模块，并由同一 Python 应用组合部署的业务核心；模块各自拥有写模型，只在出现独立安全域、运行时或容量证据时拆为远程进程。
_Avoid_: 按功能菜单拆微服务、所有模块互相直接写表、只有文件夹没有接口与所有权的“大泥球”

**临时任务胶囊**：
绑定单次运行、用途、对象范围和期限的加密最小上下文，允许云端工作节点或 Qwen 在授权范围内处理设备侧私人数据；到期、撤权、取消或删除后必须失效。
_Avoid_: 上传完整个人保险库、永久云端上下文、用一次授权换取后续无限处理

**可审计工作流**：
由监督器将任务编译为带有明确输入、输出、依赖和质量门槛的类型化流程；专业智能体通过结构化产物协作，并接受事实、安全、重试次数及人工确认规则约束。
_Avoid_: 多个智能体自由群聊、仅靠自然语言转述状态、无限自主循环

**确定性控制平面**：
位于监督器和专业工作节点之外，负责身份授权、作用域、Schema、工具注册、证据规则、风险策略、人工确认、运行账本和终止判定的非自主控制层。它的规则优先于模型输出。
_Avoid_: 把权限与校验包装成自由对话智能体、允许模型修改控制规则

**运行上下文信封**：
一次工作流及其节点携带的最小不可变执行上下文，包括主体、用户、租户、项目、对象范围、授权快照、密钥时期、记忆切片、事实锁、领域包、能力版本、预算和追踪信息。
_Avoid_: 依赖全局隐式上下文、后台任务缺少用户归属、运行时临时扩权

**类型化产物**：
智能体、确定性服务和人工节点之间交换的版本化结构化对象，带 Schema、所有者、对象域、出处、状态和内容哈希；自由文本只能作为其字段，不能代替权限、证据关系或质量结果。
_Avoid_: 智能体之间只传自然语言消息、覆盖历史版本、无来源中间结果

**质量门**：
在编译、授权、输入、节点输出、科学、教学、表达、多模态和发布阶段执行的结构化裁决点，只能返回通过、等待人工、可重试失败、闭锁失败或失效。
_Avoid_: 模型自称“已检查”、失败后跳过检查、用无限重试代替裁决

**工作流运行状态**：
描述一次编排是否处于草拟、已编译、运行、等待人工、重试、成功、阻塞或取消的状态；它与产物是否可信、批准或发布相互独立。
_Avoid_: 把任务执行完成直接解释为产物可发布

**产物可信状态**：
描述科学产物从草稿、证据绑定、合格到批准，或进入冲突、隔离、失效的状态；发布必须同时满足运行终态、可信状态和发布门。
_Avoid_: 用单一“完成”状态混合生成成功、事实正确和发布批准

**领域包**：
面向一个科学领域的可插拔知识与校验模块，定义该领域的权威来源层级、术语本体、公式与单位规则、证据强度、常见误区、专用工具和评测集。公共平台能力不复制到各领域包中。
_Avoid_: 用一套通用提示词处理所有学科、为每个学科复制整套应用

**证据化画像**：
四维画像中的可审计稳定记录，由系统按新合同自动提取低风险信号并去重写入；用户可查看内容和首次稳定记录时间、修改和撤回，内部保留必要来源、版本和撤回审计。回答时只调用当前任务所需的最小画像切片。
_Avoid_: 由模型随意改写的整段用户总结、不可追溯的隐式记忆

**四维画像**：
用户画像的唯一新产品范围：学业情况、感兴趣的知识、兴趣爱好和阶段目标。画像默认自动提取，禁止敏感推断，使用时只注入与当前问题、模式和课时相关的最小切片。
_Avoid_: 九维画像、情绪或人格结论、完整画像注入、把一次隐私说明当作逐条授权

**旧九维画像**：
退役术语。迁移时仅把高置信内容映射到四维画像，其余记录只读封存并停止注入；旧维度、候选确认和复杂历史页面不再作为新合同。
_Avoid_: 继续写入九维字段、把封存内容当作当前事实、删除迁移审计

**候选画像**：
内部过渡状态，不是用户可见的授权或确认流程。它只能承载尚未达到稳定证据门槛的低风险观察；敏感身份、健康、情绪、人格和心理推断不得进入候选或稳定画像。
_Avoid_: 将单次情绪、随口示例或敏感身份推断直接写入稳定画像

**画像观察**：
带原始来源和发生场景的原子低风险用户信号，可支持内部去重和稳定性判断；它不构成用户授权，也不是稳定用户结论。
_Avoid_: 人格标签、无来源的模型猜测、整段对话摘要

**知识状态**：
由学习记录支持的、针对某个知识点当前掌握程度及不确定性的可撤销判断，用于确定最近发展区和下一次验证任务。
_Avoid_: 课程完成率、浏览时长、不可解释的掌握百分比

**会话记忆**：
只为维持一次对话连续性而保留的短期上下文，不会自动晋升为证据化画像或跨会话事实。
_Avoid_: 将完整聊天历史默认保存为长期记忆

**项目记忆**：
历史术语。迁移后的项目决策、约束、术语、开放问题和产物关系只作为历史对象或迁移审计读取，不进入新产品的上传、检索或全局画像路径。
_Avoid_: 全局用户画像、无边界的聊天摘要

**用户置顶事实**：
用户明确要求系统保存并在指定范围内遵循的事实、边界或默认设置，其来源是用户授权而非模型推断。
_Avoid_: 系统猜测的偏好、未限定范围的永久指令

**画像自动更新许可**：
退役术语。新合同默认自动提取低风险四维画像，首次启用只显示一次非交互隐私说明，不提供授权弹窗、逐条写入通知或逐类开关；仍支持修改和撤回。
_Avoid_: 把一次隐私说明解释为授权弹窗，或把自动提取扩展到敏感推断

**画像授权范围**：
画像治理中的退役术语。新合同以四维范围、最小任务切片、禁止敏感推断和撤回墓碑限制画像使用；通用数据访问授权与账户、对象、能力的安全边界仍然有效。
_Avoid_: 把通用数据授权重新设计成画像逐类授权界面

**情境信号**：
对当前紧迫度、困惑或可能情绪的会话级低置信观察，只能用于当次交互适配，不进入长期画像，也不能形成心理诊断或稳定人格画像。
_Avoid_: 情绪画像、心理健康判断、敏感身份推断

**记忆切片**：
系统针对一次任务编译的最小、已授权且可解释的画像和记忆集合，是长期信息进入模型上下文的唯一载体。
_Avoid_: 完整画像注入、让模型直接查询全部记忆

**持续评测**：
随产品版本运行的可复现评测体系，联合确定性规则、固定模型裁判、专家评分和用户盲测，覆盖纵向个性化、科学可靠性、人味表达、风险识别、工作流稳定性、基线对比与消融实验。
_Avoid_: 仅在交付前人工挑选少量成功案例、只用模型自评

**评测套件**：
可独立发行的版本化评测资产集合，统一登记数据切片、指标、量表、裁判、基线、消融、发布门、领域包依赖、数据卡和失效状态。
_Avoid_: 散落的测试脚本、只含题目和参考答案的静态题库、无法追踪变更的排行榜

**评测案例**：
在明确用户、租户、项目、学习阶段和科学领域上下文中，带初始状态、输入、授权、允许工具、预期 Claim、合法状态路径、自动断言、人工量表和预算的最小可重放评测单元。
_Avoid_: 脱离用户与工作流上下文的单轮提示词、只检查最终文本

**评测运行锁**：
一次评测实际使用的代码与环境、数据集、领域包、模型运行锁、提示词、Schema、工作流、工具、裁判、随机种子和执行次数的不可变快照，用于新旧版本配对双跑与跨部署复现。
_Avoid_: 只记录模型名称、用当前配置猜测历史实验、不同部署模式各自生成不可比较结果

**用户账户**：
由稳定内部 ID 标识的用户数据归属边界，用户名变化不会改变画像、对话、全局知识库、教学计划、私人材料、索引、记忆与评测结果的归属；历史项目、提醒和扩展迁移记录也按账户隔离。用户进入功能页面前必须完成注册和登录，不同账户的数据不得互相召回或影响。
_Avoid_: 匿名共享画像、跨用户合并记忆、无归属的后台任务

**用户名**：
用户可修改的公开账户名称，经规范化后按大小写不敏感方式保持唯一，可与密码共同用于登录。
_Avoid_: 账户主键、区分大小写的重复名称、改名后创建新账户

**QQ 邮箱**：
由数字 QQ 号和 `@qq.com` 组成、在全部账户中保持唯一且可与密码共同用于登录的联系地址。
_Avoid_: 任意邮箱、Foxmail 别名、多个账户共用同一地址

**任务安排**：
退役术语。历史任务和提醒只读保留在账户导出与迁移审计中，不再创建、调度、补发或转化为教学计划。
_Avoid_: 把后台任务执行器误称为用户任务安排，或恢复提醒发送入口

**QQ 邮件凭据**：
退役凭据。历史 SMTP 授权码在迁移时幂等清除，不进入导出、日志或模型上下文；QQ 邮箱作为账户登录标识的规则不因此改变。
_Avoid_: 恢复提醒发送、恢复用户 SMTP 配置页面、把授权码当登录密码

**学习使命**：
用户学习某个主题背后的具体真实目标，包括可观察的成功标准、约束和明确范围；教学内容与下一步选择必须能追溯到学习使命。
_Avoid_: 抽象的“了解某主题”、与用户真实目标无关的课程堆叠

**学习记录**：
用户已经展示出理解、纠正了误区或明确具备某项先备能力的证据化记录，用于判断最近发展区和调整后续学习路径。
_Avoid_: 浏览记录、课程覆盖日志、未经验证的“已掌握”

**学习路径**：
由学习使命、先备知识、知识掌握度、学习记录与反馈共同驱动的逐用户教学路线；它会随真实学习证据更新。
_Avoid_: 所有用户共享的固定课程目录

**教学计划**：
由学习目标、四维画像的最小相关切片、全局知识库证据和必要联网证据驱动的持久教学路线；计划创建的同一回复必须交付第一课时。
_Avoid_: 学习项目页面、任务安排、没有证据的静态课程目录

**课时**：
一次有效教学回复的可审计进度单位；每次最多推进一个课时，澄清、失败和重试不推进，测验表现和用户反馈只影响后续课时。
_Avoid_: 把生成成功、浏览时长或重试次数当作课时完成

**能力注册表**：
记录受控能力的逻辑用途、适用边界、区域、版本、限制、成本快照、验证状态与替代关系的版本化目录；用户通过自然语言进入能力路由，不能直接管理内部注册项。
_Avoid_: 散落在提示词中的模型名、默认相信稳定别名、供应商能力清单

**能力路由**：
将用户自然语言意图映射到能力注册表中受控能力的确定性入口；低置信时只追问一个必要澄清问题，计费能力不得因随口提及误触发，路由结果必须随运行持久化。
_Avoid_: 用户选择 SKILL/MCP、手动工具菜单、失败后静默改走另一能力

**内置 SKILL**：
随 BridGes 发布、只读、由维护者登记和审计的能力说明、参考资料与模板；它是内部能力组成部分，不是用户可上传、安装、选择或修改的扩展。
_Avoid_: 用户扩展中心、动态安装包、把内置身份当作跳过数据最小化和质量门的理由

**紧凑引用**：
回答实际采用全局知识库或公开来源时展示的最小可定位来源信息；不展示独立的本地检索过程卡、分层来源状态或手动检索控件。
_Avoid_: 虚构文件名、把检索成功状态当作引用、用完整内部来源图替代可理解定位

**全局百炼运行凭据**：
BridGes 正式运行的唯一 Qwen 认证来源，由维护者在启动前通过 `BRIDGES_QWEN_API_KEY` 或 `BRIDGES_QWEN_API_KEY_FILE` 配置（文件引用优先用于容器或长期部署）；缺失、为空或不可读时启动硬门失败关闭，不再启动只能登录、不能使用核心能力的降级实例。只校验必需值可读取，不发起可能计费的全量真实探测（ADR-0024）。
_Avoid_: 账户级百炼密钥、`.env` 明文、日志或接口响应中的明文、账户间共享、数据导出携带密钥

**账户能力状态**：
旧合同遗留概念（ADR-0005 已废弃，GQ-06/GQ-07 已清退）：账户级百炼密钥的逐项真实能力探测结果；清退按现有账户 ID 幂等删除旧凭据与 `key_metadata`/`key_probes` 状态，历史审计事件保留作遗留证据。新实现不得以任何账户探测快照判定能力可用性。
_Avoid_: 恢复账户级密钥用户面或探测门禁、把探测状态当作新能力可用性依据、配置即已验证、Stub 成功、用一种能力成功推断全部模型可用

**固定模型绑定**：
一个逻辑能力类别只绑定一个经评测的具体模型快照；供应商未提供快照时，只能绑定一个带能力合同与漂移门禁的模型 ID。失败时只能重试同一模型，升级或合同变化必须重新评测。
_Avoid_: 不受控的稳定别名、隐藏备用模型、运行时自动切换、用户选择模型

**索引版本**：
由 Embedding 模型 ID、向量维度、文本规范化、分块器和元数据 Schema 共同确定的不可混写索引边界；任一合同变化都必须新建、重建并验证后再切换。
_Avoid_: 原位改维度、不同模型共用向量空间、未验证即覆盖旧索引

**模型运行锁**：
一次工作流实际使用的能力版本、区域、参数、输入输出契约、提示与降级路径的不可变快照，用于复现、评测、审计和迁移对比。
_Avoid_: 只记录“使用了 Qwen”、事后按当前配置猜测历史运行

**统一运行合同**：
源码环境中的统一 CLI、Docker 与 Podman 都必须遵守的配置、迁移、健康检查、存储和服务生命周期语义；运行载体可以不同，系统行为与数据边界不能不同。
_Avoid_: 每种部署方式维护一套独立规则、只保证容器路径可用

**源码环境部署**：
用户下载项目源码后创建独立 Conda 或 `.venv` 环境，升级 pip，依据项目锁定文件安装后端与前端依赖，再激活环境、进入项目目录并执行 `BridGes start` 的正式非容器部署路径；它与 Docker 部署地位并列，不提供 Windows 原生安装包，也不要求用户创建 `.env`。
_Avoid_: 预编译桌面安装器、依赖未经锁定的全局 Python 或 Node 包、跳过前端构建、用手工启动多个服务替代统一 CLI

**个人保险库**：
用户个人画像、学习记录、全局记忆、私人材料和个人索引的默认归属域；加入项目或机构不会让其他成员或管理员继承其可见性。
_Avoid_: 个人租户、管理员可见的用户空间、机构成员目录

**显式共享项目**：
由用户主动加入对象、按项目角色与对象授权协作的科学项目空间；个人对象进入其中时默认生成独立且最小化的项目副本，不连带个人保险库。
_Avoid_: 自动共享空间、个人保险库的多人视图、整个账户共享

**机构管理域**：
机构身份、席位、策略、机构自有项目和必要审计的治理边界；它可以管理成员关系，但默认不能浏览成员的个人保险库。
_Avoid_: 管理员超级账户、机构拥有成员全部数据、个人账户上级容器

**设备副本**：
绑定用户、设备、对象域、授权版本与密钥时期的加密离线副本；它不是独立所有权域，也不能在撤权或删除后成为权威真相。
_Avoid_: 离线主库、无法撤权的永久副本、设备即所有者

**领域包专家工作台**：
领域专家、独立复核者、平台发行者和安全管理员共同完成领域包来源登记、规则编辑、夹具验证、语义差异审查、签名、灰度、发行、失效处置和回滚的职责分离工作界面；主结构采用阶段控制台，并嵌入语义 Diff、责任泳道和失效影响视图。
_Avoid_: 普通内容管理后台、一个批准按钮覆盖全部流程、同一角色编辑并批准

**规范化包摘要**：
对领域包 Manifest、内容树、依赖锁、夹具摘要、构建出处和语义 Diff 进行确定性规范化后得到的签名对象；其中任一内容变化都会产生新摘要并使旧签名不再适用。
_Avoid_: 只签压缩包文件名、允许签名后修改内容、不同角色各签不同版本

**领域包发行签名链**：
内容维护者的内容签名、独立复核者的独立验证签名和平台发行者的发行签名共同组成的职责分离证明；三者必须绑定同一规范化包摘要，高风险变更还需满足领域资质与安全治理联合签名。
_Avoid_: 模型投票代替独立复核、单人多角色签名、安全管理员直接发布

**领域包失效事件**：
来源撤回、关键依赖失效、规则缺陷或安全事件触发的独立治理对象，记录影响集、暂停或撤销动作、下游传播、重验证进度和关闭条件，不得通过覆盖历史版本消除。
_Avoid_: 静默替换旧包、只改一个状态字段、不追踪既有运行与产物影响

**成熟度门**：
控制成熟项目开发阶段推进的证据化裁决点，综合检查进入条件、跨模块合同、迁移、科学安全、评测、容量恢复、文档和部署就绪；它描述系统成熟度，不是日历节点或对外 MVP 版本。
_Avoid_: 按工期自动通过、用完成百分比抵消硬失败、先进入下一阶段再补证据

**开发责任域**：
围绕稳定领域对象、接口和写模型建立的长期团队所有权边界；责任域可跨阶段并行，但生产者、消费者和独立门禁责任必须分离。
_Avoid_: 按临时页面分组、多个团队直接写同一权威模型、构建团队独自签署全部门

**内部集成版本**：
由锁定构建产生、仅用于验证当前成熟度门的可运行内部版本，必须具备合成多用户环境、迁移、健康检查、观测和当前阶段评测证据；未达到成熟发行基线时不得包装为完整对外产品。
_Avoid_: 关闭权限换取可运行、把内部版本称为 MVP、用演示成功代替系统验收

**成熟发行基线**：
完整产品能力通过 G0 构建完整、G1 确定性正确、G2 安全科学硬门、G3 质量非劣、G4 人工有效和 G5 运营就绪后形成的首个正式发行资格。
_Avoid_: 带关键风险发布、只通过功能测试、生产部署语义不一致

**规范性归属**：
最终规划书中某个稳定概念、合同、状态或规则的唯一权威章节；其他章节只能通过稳定 ID、摘要和交叉引用使用它，附录不得创建冲突规则。
_Avoid_: 同一概念多章重复定义、示例静默变成规范、附录覆盖正文

**黄金路径**：
贯穿最终规划书的具名端到端用户与系统旅程，包括个性化学习、科学表达、多模态创作、画像治理、协作治理和失效恢复；它解释章节连接关系，但不替代各权威章节的规则。
_Avoid_: 只含 happy path 的演示脚本、每章重新编造案例、用用户旅程省略横切合同

**文档覆盖矩阵**：
把比赛要求、用户硬约束和已确认决策映射到唯一正文落点、领域对象、图表、测试评测、附录和责任域的追踪表，用于证明完整性和发现重复或遗漏。
_Avoid_: 只按评分项重复写正文、没有验收证据的勾选表、用矩阵创建新架构

**受信任内置 SKILL**：
随 BridGes 一同发布、由项目维护者评审并以只读版本安装的能力说明、静态参考资料、模板与资源；其版本进入能力注册表和运行审计，用户不能安装、选择或修改，仍受数据最小化与质量门约束。
_Avoid_: 用户可修改的系统提示、无需授权读取个人保险库、以“内置”为由跳过版本和审计

**声明式用户 SKILL**：
退役术语。历史用户扩展包先停用并隔离为不可执行，清单和审计封存；兼容窗口结束且导出验证后清理正文。
_Avoid_: 恢复用户扩展中心、安装、选择、运行或以资源文件夹逃逸读取宿主文件

**MCP 权限清单**：
退役术语。用户不再安装、批准、选择或管理 MCP；历史清单、会话选择和审计只用于停用迁移与导出，不恢复执行。
_Avoid_: 继承应用全部权限、直接读取百炼或 SMTP 凭据、访问完整个人保险库、后台静默扩大权限

**用户插件/MCP**：
退役产品能力。用户不能上传、安装、启用、选择、调用或管理插件、SKILL 和通用 MCP；历史清单、包和调用审计只用于迁移、导出与安全核查。
_Avoid_: 以兼容为由恢复用户扩展执行，或删除仍需审计的历史清单

**BridGes 人性化表达 SKILL**：
以原创规则、提示、示例和评测资产实现的只读内置 SKILL，同时支持改写已有文本与按主题生成文本；输出必须包含最终文本、修改明细与理由、事实核查结果和尚未解决的问题，并始终受事实锁、引用和任务契约约束。
_Avoid_: 规避 AI 检测、冒充真人或特定名人、复制无许可证项目内容、以自然流畅为由删除限定条件和证据

**来源清洁记录**：
记录人性化表达规则、示例、代码与测试资产是原创、仅受通用方法启发，还是依法复用了带许可证材料的可审计清单；任何实际复用都必须保留对应许可证、署名和来源关系。
_Avoid_: 把浏览过的参考仓库视为可自由复制、事后补猜来源、用改写措辞掩盖未授权复制

**单机权威数据库**：
以 SQLite 持久化账户、对话、四维画像、全局知识库、教学计划、运行记录、导出和迁移审计的本地权威状态；学习项目、提醒和用户扩展只作为迁移中的历史对象，启用外键、事务、迁移与账户作用域约束。
_Avoid_: 进程内字典充当持久层、为单机首版引入 PostgreSQL、不同部署方式使用不同数据模型、用显示名称作为数据归属键

**本地对象库**：
按账户归属并加密保存上传材料、生成图片、视频、语音和导出文件的本地内容目录；数据库只保存对象 ID、所有者、摘要、版本、媒体类型和受控路径，所有访问必须经过对象域授权。
_Avoid_: 把大文件塞进数据库、浏览器直读任意宿主路径、用用户文件名充当真实存储路径、跨账户共享未授权对象

**本地后台执行器**：
与 Web 服务属于同一安装和数据边界、但作为独立受监督进程运行的任务执行组件，负责可恢复的生成、索引、迁移和清理任务；不再调度或发送用户提醒，统一 CLI 或容器入口管理其启动、健康检查和停止。
_Avoid_: 在请求线程执行长任务、依赖 Redis 或云调度才能启动、恢复退役提醒调度、重启后丢失任务状态

**原型数据封存**：
将不含真实账户、用户上传材料或有效画像的旧 Science Companion 数据库作为只读历史快照保留，并以全新 `bridges.db` 启动 BridGes 权威数据模型；旧业务对象、Stub 状态和测试数据不进入新库。
_Avoid_: 在旧库上原位改造、为不存在的真实数据维护自动迁移器、把测试状态解释为用户事实、删除唯一历史快照

**云端任务切片**：
一次 Qwen 请求所需的最小可审计数据集合，只能包含当前问题、必要的可见会话片段、当前模式允许的最小四维画像切片、相关全局知识库段落和必要工具结果；首次启用画像只展示一次非交互隐私说明，内部保留披露审计。
_Avoid_: 完整聊天历史、完整个人保险库或项目目录、敏感推断、百炼或 SMTP 凭据、为了方便而打包全部可用上下文

**搜索查询切片**：
从当前任务提炼并在本地去除身份与私人材料后发送给 DuckDuckGo 的最小关键词集合；搜索结果返回本地后才能与私人上下文组合，原始文档、完整问题和画像不得发送给搜索服务。
_Avoid_: QQ 邮箱或用户名、原始私人文档、完整提示词、把本地检索片段作为搜索参数、将组合后的私人上下文回传搜索服务

**云端披露记录**：
记录一次外部请求使用的账户、能力、模型或服务、时间、授权快照、数据类别、来源对象 ID、响应状态和追踪标识的本地审计记录；默认不复制保存完整敏感请求正文。
_Avoid_: 在日志中记录密钥或密码、以完整提示词代替数据类别审计、缺少账户归属、无法关联授权与来源

**BridGes 纵向替换切片**：
在当前仓库中从用户界面、API、领域模型、持久化、外部能力到验收测试完整交付的一段 BridGes 用户旅程；首个切片覆盖注册或登录、配置全局百炼运行凭据、新建对话、流式回答、保存消息和恢复对话，验收后才逐步替代旧产品路由。
_Avoid_: 前后端长期分离交付、先重写全部基础设施、继续扩展旧工作台、没有真实模型与持久化的演示壳

**旧产品退出门**：
旧 Science Companion 页面或模块被移除前必须满足的替代条件，包括对应 BridGes 纵向能力已通过功能、安全、账户隔离、迁移和回归验收；退出门只保护仍有复用价值的工程骨架，不承诺保留旧信息架构和旧业务模型。
_Avoid_: 一次性删除全部可复用骨架、为了兼容继续暴露旧工作台、用占位页声称已替代、没有回归证据即删除

**QQ 邮箱控制权验证**：
退役提醒能力的历史状态；迁移后不再创建或更新 SMTP 验证，也不影响账户登录、聊天、资料管理和导出。
_Avoid_: 恢复提醒验证、把格式校验称为控制权验证、用 SMTP 授权码登录、未验证邮箱时禁用本地聊天

**设备账户会话集**：
同一 BridGes 本地设备上由用户逐个输入凭据建立、彼此独立且可撤销的账户会话集合；有效会话之间可一键切换活动账户，会话过期或被移除后必须重新输入目标账户密码，切换不得改变任何数据归属。
_Avoid_: 多账户共享一个授权主体、切换时合并对话或画像、仅隐藏界面而不切换后端账户、退出一个账户即泄露其他账户状态

**敏感操作再认证**：
导出或删除数据等敏感操作执行前，对当前账户近期密码验证状态的强制检查；普通聊天和有效会话切换不重复触发该门。
_Avoid_: 只凭长期设备 Cookie 放行、用另一个账户的近期认证放行、把历史 SMTP 授权码当登录密码、为每条普通消息重复要求密码

**提醒补发窗口**：
退役术语。历史提醒不再补发；迁移只取消未来任务和待发邮件，保留已发送历史的账户导出。
_Avoid_: 声称程序关闭时仍会发送、启动后发送全部积压邮件、静默恢复退役任务

**提醒投递记录**：
历史审计对象。按账户保留已发送投递和取消结果供导出与迁移核查，不再生成新的发送、补发或重试记录。
_Avoid_: 无限重试、把排队等同于已发送、记录 SMTP 授权码、跨账户合并发送状态、恢复退役发送能力

**分层检索范围**：
退役术语。新合同使用“全局知识库检索”：只从当前账户知识库读取，按能力和意图按需触发；实际采用材料时展示紧凑引用，公开来源另按联网证据合同处理。
_Avoid_: 聊天附件、学习项目文件或局部知识库作为新检索域，跨账户检索、把模型记忆伪装成检索证据、恢复旧证据工作台

**教学证据充足性门**：
学习模式在形成正式教学回答前，对当前账户全局知识库是否足以覆盖教学目标作出的结构化裁决；材料缺失、过时、证据不足或无法完成任务时必须自动调用经登记的公开联网来源补充，并在仍不足时明确说明缺口。
_Avoid_: 没有材料就只凭模型记忆教学、让用户必须手动要求联网、用搜索成功代替证据充足、来源不足时伪造确定结论

**教学自动联网**：
教学证据充足性门未通过后自动执行、对用户可见且遵守搜索查询切片边界的 DuckDuckGo、arXiv 或其他已登记公开来源检索；该强制规则只适用于学习模式教学任务，日常陪伴模式仅在用户明确要求、信息具有时效性或事实核查必要时联网。
_Avoid_: 普通闲聊无条件搜索、把私人原文发送给搜索服务、隐藏搜索过程、搜索失败后继续声称已完成可靠教学

**数字分身**：
由四维画像和可选静态头像组成的轻量个人画像视图，集中呈现学业情况、感兴趣的知识、兴趣爱好和阶段目标；每条内容只支持查看内容与首次稳定记录时间、修改和撤回，来源、冲突、版本和撤回墓碑留在内部去重与审计域。
_Avoid_: 3D 虚拟人、实时动画、口型同步、声音克隆、用头像推断性格或情绪、不可解释的整段人格总结

**静态分身头像**：
由用户上传或调用已登记图片模型生成、仅用于账户视觉识别的静态图像对象；它属于当前账户，可以替换或删除，但不得成为身份真实性、画像结论或情境信号的证据。
_Avoid_: 生物识别凭据、心理推断输入、跨账户共享、自动生成动画或声音、删除账户后继续保留

**对话固定模式**：
随对话持久化并决定后续消息编排规则的日常陪伴或学习模式；新聊天在首条消息提交前选择，首条用户消息提交时由服务端永久锁定，另一种模式必须新建会话。
_Avoid_: 每条消息重复选择、首条消息后切换、客户端绕过服务端锁、把学习项目变成固定课程工作台、两种模式使用彼此隔离的聊天系统

**模式切换事件**：
退役术语。历史事件只读保留用于审计和历史渲染，新产品不产生模式切换写事件；需要上下文隔离时新建会话。
_Avoid_: 无服务端锁的隐式切换、追溯改变旧消息语义、由模型未经用户操作改变模式

**电脑端运行边界**：
BridGes 只允许在电脑上通过源码 Conda/`.venv` 环境或 Docker/Podman 部署，并只面向桌面浏览器交互；界面围绕可折叠桌面侧边栏、键盘操作、常见电脑分辨率和浏览器缩放设计。
_Avoid_: 手机或平板适配、移动浏览器、触屏专用布局、PWA、原生移动应用、以移动抽屉替代电脑端导航
